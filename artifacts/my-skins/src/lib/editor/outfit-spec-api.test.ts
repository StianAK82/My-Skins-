import test from "node:test";import assert from "node:assert/strict";
import {outfitSpecUrl,requestOutfitSpec,parseGenerateOutfitSpecResponse} from "../outfit-spec-api.ts";
const spec={requestLanguage:"en",originalPrompt:"White hoodie skin",normalisedPrompt:"white hoodie skin",outfitName:"White Hoodie",overallStyle:"minimal",palette:["#ffffff"],top:{id:"top",category:"hoodie",slot:"top",color:"#ffffff",material:"cotton-fleece",construction:{fit:"regular",torsoWidth:1.12,torsoLength:1.1,torsoDepth:.68,shoulderDrop:.12,sleeveLength:1,sleeveFullness:1,cuffHeight:.16,waistbandHeight:.14,hoodHeight:.96,hoodDepth:.44,hoodOpeningWidth:.43,hoodOpeningHeight:.66,pocketType:"kangaroo",pocketWidth:.72,pocketHeight:.32,drawstringEnabled:true,drawstringLength:.38}},bottom:null,footwear:{category:"none",previewOnly:true},graphics:[],classicExportPlan:{shirt:true,pants:false,width:585,height:559,exportType:"roblox-classic",previewRepresentation:"procedural-hoodie",hasReal3DExport:false},safetyDecision:"allow",modelConfidence:.98,generationSeed:"test"};
const response={generationId:"11111111-1111-4111-8111-111111111111",outfitSpec:spec,validation:{schemaValid:true,safetyApproved:true,mandatoryParts:["hood"],missingParts:[],severeIntersections:[],classicValid:true},classicExports:[{type:"shirt",fileName:"shirt.png",mimeType:"image/png",width:585,height:559,url:"data:image/png;base64,AA=="}],diagnostics:{repairAttempts:0}};
test("Create generation posts to the production outfit-spec endpoint",async()=>{let url="",init:RequestInit|undefined;const fetcher=async(input:RequestInfo|URL,next?:RequestInit)=>{url=String(input);init=next;return new Response(JSON.stringify(response),{status:200});};const result=await requestOutfitSpec("White hoodie skin",new AbortController().signal,fetcher as typeof fetch);assert.equal(url,"/api/ai/outfit-spec");assert.equal(init?.method,"POST");assert.deepEqual(JSON.parse(String(init?.body)),{prompt:"White hoodie skin"});assert.equal(result.outfitSpec.top.construction.pocketType,"kangaroo");assert.equal(result.classicExports.length,1);});
test("base paths cannot select the legacy or revision endpoint",()=>{assert.equal(outfitSpecUrl("/api"),"/api/ai/outfit-spec");assert.equal(outfitSpecUrl("/app/"),"/app/api/ai/outfit-spec");});
test("client rejects a response property mismatch",()=>assert.throws(()=>parseGenerateOutfitSpecResponse({...response,outfitSpec:undefined,generatedOutfitSpec:spec}),/incompatible response/));
