Place a Roblox R15-compatible avatar model here as:

- roblox-r15.glb

The preview system auto-detects this file and uses it when present. If absent, the app falls back to a blocky Roblox-style avatar rig so preview remains functional.
