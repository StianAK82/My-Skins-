# Classic artifact pipeline

The server compiler produces deterministic 585×559 RGBA PNGs from validated design state, excludes preview-only layers, validates dimensions/alpha/content/zone coverage, hashes bytes, and stores object-backed artifacts. Canonical resolution rejects arbitrary URLs and remote input is never fetched. Quarantine and deletion are tested. Production storage must fail closed when unavailable; moderation and rights remain explicit evidence-based decisions.
