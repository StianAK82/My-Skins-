# Outfit quality gate

`scoreOutfitQuality` reports fifteen named 0–100 dimensions, their arithmetic mean, threshold, acceptance, and reasons. Preview or export validity of zero is critical and fails closed. A score is evidence from deterministic validators, not proof of subjective visual excellence. Release threshold defaults to 78; any unresolved failure reason prevents acceptance.
